export default class User {
  constructor(
    public id: number,
    public name: string,
    public userName: string,
    public email: string
  ) {}

//   isAdult() {
//     return this.age > 36 && this.age < 60
//   }
}
